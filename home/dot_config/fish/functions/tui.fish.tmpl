{{- /* Launch tremc TUI for transmission */ -}}
function tui --description "Launch transmission TUI (tremc)"
    if not systemctl --user is-active --quiet transmission-daemon
        echo "⚠ Transmission daemon not running. Starting..."
        tstart
        sleep 2
    end

    # Launch tremc with connection to local daemon with credentials
    tremc --netrc
end