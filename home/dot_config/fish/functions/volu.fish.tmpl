function volu --description "Increase volume by 5% (volume-up)"
    # Ultra-Zen Philosophy: Volume Adjustment
    # "Sound rises with intention"

    # Use pamixer for quick CLI volume control
    if command -v pamixer >/dev/null 2>&1
        pamixer -i 5
        # Show current volume
        echo "Volume: $(pamixer --get-volume)%"
    else
        echo "pamixer not found. Install it or use 'vol' for TUI control."
        return 1
    end
end