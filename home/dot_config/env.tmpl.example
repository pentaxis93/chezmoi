# Example: Environment Variables with Secrets from Bitwarden
#
# This is an EXAMPLE file showing how to manage environment variables with secrets.
# To use:
# 1. Store your secrets in Bitwarden with appropriate names
# 2. Rename this file to match your needs (e.g., '.env.tmpl' for a project)
# 3. Update the Bitwarden item names below
# 4. Run 'chezmoi apply' to generate the environment file

# GitHub Personal Access Token
{{- with bitwarden "item" "github-pat" -}}
GITHUB_TOKEN={{ .login.password }}
{{- end }}

# OpenAI API Key
{{- with bitwarden "item" "openai-api" -}}
OPENAI_API_KEY={{ .login.password }}
{{- end }}

# Database Connection String
{{- with bitwarden "item" "database-prod" -}}
DATABASE_URL={{ .notes }}
{{- end }}

# Custom API with multiple fields
{{- with bitwardenFields "item" "custom-api" -}}
API_ENDPOINT={{ .endpoint.value }}
API_KEY={{ .api_key.value }}
API_SECRET={{ .api_secret.value }}
{{- end }}

# Example of using the password template helper
SOME_PASSWORD={{ template "bitwarden-password.tmpl" "service-account" }}