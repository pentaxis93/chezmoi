# Example: AWS Credentials from Bitwarden
#
# This is an EXAMPLE file showing how to manage AWS credentials with Bitwarden.
# To use:
# 1. Create a Bitwarden item for each AWS profile with custom fields:
#    - Field name: "aws_access_key_id"
#    - Field name: "aws_secret_access_key"
# 2. Rename this file from 'credentials.tmpl.example' to 'credentials.tmpl'
# 3. Update the item names below to match your Bitwarden items
# 4. Run 'chezmoi apply' to generate the credentials file

{{- /* Example for default profile */ -}}
{{- with bitwarden "item" "aws-default" -}}
[default]
aws_access_key_id = {{ (bitwardenFields "item" "aws-default").aws_access_key_id.value }}
aws_secret_access_key = {{ (bitwardenFields "item" "aws-default").aws_secret_access_key.value }}
{{- end }}

{{- /* Example for work profile */ -}}
{{- /* Uncomment and modify as needed:
{{- with bitwarden "item" "aws-work" -}}

[work]
aws_access_key_id = {{ (bitwardenFields "item" "aws-work").aws_access_key_id.value }}
aws_secret_access_key = {{ (bitwardenFields "item" "aws-work").aws_secret_access_key.value }}
{{- end }}
*/ -}}